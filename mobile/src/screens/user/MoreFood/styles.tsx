import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    titleContainer: {
        marginVertical: 10,
        marginLeft: '6.5%',
    },
    foodContainer: {
        alignItems: 'center',
    },
})

export default styles
